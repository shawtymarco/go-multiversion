# Minecraft 1.21.5x mapping audit

The target snapshots contain 15,162 block states, 1,789 items, and 1,954 creative entries. Direct native mapping records 2,337 block fallbacks, 261 native-only item fallbacks, and one target-only item (`minecraft:debug_stick`). Target-only items stay advertised for registry fidelity and are rejected serverbound.

The target air runtime ID is 11,996. Creative selection network IDs remain owned by the current Dragonfly creative resolver while stack and block IDs are translated semantically.
