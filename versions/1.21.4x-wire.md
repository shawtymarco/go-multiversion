# Minecraft 1.21.4x wire audit

Protocol 748 is locked to gophertunnel `268adeb5710990ebd95edda3b42b0cf13f58bf8b`. Relative to 766 it restores uint64 PlayerAuthInput flags, the pre-filter ItemStackResponse, string resource-pack UUIDs without world-template fields, the older camera aim-assist packet, and the packet tail ending at SetMovementAuthority. StructureTemplateDataRequest is absent from the client pool exactly as in the final target snapshot.

Generated zero-pool fixtures and populated fixtures pass byte-identical round trips through an executable built from the locked historical module.
